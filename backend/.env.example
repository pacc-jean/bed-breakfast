# Local dev port (Render injects its own in production)
PORT=5000

# Email service credentials (Gmail App Password setup)
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-generated-app-password
HOST_EMAIL=your-email@gmail.com
